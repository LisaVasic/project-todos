{
  "name": "project-todos",
  "version": "1.0.0",
  "description": "The asignment was to build a todo app using React and Redux. In the app you should be able to add, check/uncheck and delete a task. The app should also count either all tasks or the uncompleted ones.",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-scripts": "^5.0.1"
  }
}
